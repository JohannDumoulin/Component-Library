import { defineConfig } from 'windicss/helpers'

export default defineConfig({
    theme: {
        fontFamily: {
            'DynaPuff': 'DynaPuff',
            'PassionOne': 'PassionOne'
        }
    }
})
